import { environment } from 'src/environments/environment';

export const API_COUNT = environment.url + '/api/custom/external/count';
export const API_NEWS = environment.url + '/api/custom/external/news';
export const API_CONTENT = environment.url + '/api/custom/external/contents';
export const API_FEEDBACK = environment.url + '/api/custom/external/feedbacks';
export const API_EMPLOYEES = environment.url + '/api/custom/external/employees';
